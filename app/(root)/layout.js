// app/(root)/layout.js - Layout for main site pages
// Note: Header, Navigation, and Footer are included directly in page.js files

export default function MainSiteLayout({ children }) {
  return children;
}
